import StateManager from "../../StateManager";

export function constructBuilding(currentState: StateManager) {
  let gold = currentState.fetch("gold");
  let currentBuildings = currentState.fetch("buildings");
}
